---
# Default variables for the K8s LSTM Data Pipeline role
project_root: "{{ playbook_dir }}/k8s-lstm-project"
pipeline_dir: "{{ project_root }}/data-pipeline"
namespace_name: ml-pipeline
docker_image_name: metrics-collector
docker_image_tag: latest
docker_registry: your-registry
prometheus_url: http://localhost:9090
pv_size: 10Gi
pv_storage_class: standard
pv_host_path: /mnt/data
push_image: false